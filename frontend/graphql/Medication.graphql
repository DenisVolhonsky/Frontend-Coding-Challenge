# TODO: implement all required fragments, queries and mutations similar to the patient list task. Have a look at the backend-schema.graphql for more api insights.
query ListPatientMedications($patientId: String!) {
  listPatientMedications(patientId: $patientId) {
    patientId
    labeler
    productCode
    packageCode
    brand
  }
}